/* Module 11 — Advanced Protocol Systems (lessons 48-52) */
(function (L) {

function lesson(id, num, title, minutes, summary, objectives, body, code, lab, quiz, tasks, resources) {
  L.push({ id, module: 11, num, title, level: 'Advanced', minutes, summary, objectives, body, code, lab, quiz, tasks, resources });
}

lesson('advanced-mev', 48, 'MEV and the Transaction Supply Chain', 80,
  'Follow a transaction from wallet to block, quantify sandwich risk, and choose protections that match the value at stake.',
  ['Trace public and private transaction orderflow', 'Explain priority gas auctions, proposer-builder separation and MEV-Boost', 'Recognise sandwiches, liquidations and back-running', 'Choose slippage, private-orderflow and batch-auction mitigations'],
  `<h3>Ordering is a market</h3><p>A transaction is not executed the moment a wallet signs it. It travels through a supply chain: wallet, RPC, mempool or private relay, builder, proposer and validator. Anyone who sees a profitable ordering opportunity may compete to capture it. <strong>MEV</strong> is the value available from changing, inserting or excluding transactions; it is not automatically an exploit, but its distribution changes user execution and validator incentives.</p>
<h3>The common shapes</h3><p>A sandwich buys before a known swap, lets the victim trade at the moved price, then sells after. Liquidation bots race to repay unsafe debt for a bonus. Back-running can rebalance an AMM after a large trade. Searchers bid for inclusion using priority fees or builder payments; proposer-builder separation moves the block-construction auction away from the validator, but does not remove ordering power.</p>
<h3>Mitigation is a design choice</h3><p>Set a tight <code>minOut</code> and deadline on every swap. For material trades, use private orderflow or a solver/intent system that competes for the best execution instead of exposing the exact trade to a public mempool. Batch auctions, frequent batch clearing and commit-reveal reduce the advantage of seeing an order early. None of these guarantees a fair price: measure execution quality and understand the party you now trust.</p>`,
  [{ lang: 'javascript', file: 'slippage-bound.js', caption: 'A signed minOut limits the worst execution price, including a sandwich that lands before the swap.', src: 'function minOut(quote, bps) { return quote * (10_000 - bps) / 10_000; }\n\nconst quote = 1_974; // quote from the pool at signing\nconst bound = minOut(quote, 50); // 0.50% tolerance\n\n// The router must revert when actualOut < bound. A quote in the UI alone\n// gives a searcher no limit; the signed condition does.' }],
  'mevflow',
  [{ q: 'What makes a sandwich possible?', options: ['The victim trade reveals predictable price impact before it executes', 'Validators cannot see transactions', 'AMMs have no prices', 'A wallet uses a deadline'], answer: 0, why: 'A public transaction with loose slippage advertises a trade that can be profitably moved around. The attacker needs ordering access and a route whose economics cover fees.' }, { q: 'What does a private relay guarantee?', options: ['The best possible price', 'That the transaction is hidden from every participant', 'A different orderflow and trust boundary, not a universal execution guarantee', 'No block fees'], answer: 2, why: 'Private submission can reduce public-mempool exposure, but the relay or builder still has a role. Evaluate inclusion, censorship and execution-quality assumptions.' }, { q: 'Which control belongs in the signed swap?', options: ['A marketing quote', 'minOut and a deadline', 'The current gas-price chart', 'A validator nickname'], answer: 1, why: 'The contract can only enforce data in the transaction. minOut encodes the worst acceptable output; deadline bounds how long the order remains valid.' }],
  ['Use the lab to see how wider slippage increases sandwich room.', 'Compare a public, private-relay and intent-based trade flow for a high-value swap.', 'Write a transaction-orderflow threat model for your dApp.'],
  [{ type: 'docs', title: 'Ethereum.org — MEV', url: 'https://ethereum.org/en/developers/docs/mev/' }, { type: 'docs', title: 'Flashbots documentation', url: 'https://docs.flashbots.net/' }]);

lesson('advanced-xchain', 49, 'Cross-Chain Messaging and Bridge Security', 80,
  'Design messages that survive finality differences, replay attempts and remote-chain failures without treating a bridge as a magical token transfer.',
  ['Separate transport, verification and application authorization', 'Compare canonical bridges, multisigs and light-client verification', 'Prevent replay, wrong-domain and duplicate-execution bugs', 'Model finality and liquidity risk across chains'],
  `<h3>A bridge carries a claim, not trustlessness by default</h3><p>Cross-chain applications have three layers: transport moves a message, verification decides whether the destination believes it, and the receiving application decides what that verified message may do. A canonical rollup bridge inherits the rollup protocol’s proof path. A multisig bridge inherits the key holders. A light client verifies remote consensus but must correctly implement headers, validator changes and finality.</p>
<h3>Messages need a domain and a lifetime</h3><p>Bind every message to source chain, destination chain, sender, receiver, nonce and payload. Store consumed message IDs before any external call. Never assume a source transaction is final simply because it has a few confirmations: reorg depth and economic finality differ by chain. A fast bridge usually fronts liquidity while it waits for finality, adding liquidity-provider and inventory risk.</p>
<h3>The receiving app is the final firewall</h3><p>Verification only says the remote message existed. The destination contract must allow only the expected remote sender, reject duplicate nonces, limit amounts and make upgrades deliberate. The largest bridge failures combine huge custody with a weak verification or authorization assumption, so caps, pausing and observability are product features, not paperwork.</p>`,
  [{ lang: 'solidity', file: 'MessageReceiver.sol', caption: 'Replay protection and domain binding belong at the receiving boundary.', src: 'mapping(bytes32 => bool) public consumed;\n\nfunction receiveMessage(uint256 sourceChain, address sourceSender, uint256 nonce, bytes calldata payload) external {\n    require(msg.sender == verifier, "only verifier");\n    require(sourceChain == EXPECTED_SOURCE && sourceSender == remoteApp, "wrong domain");\n    bytes32 id = keccak256(abi.encode(sourceChain, sourceSender, nonce, payload));\n    require(!consumed[id], "replay");\n    consumed[id] = true; // effects before application call\n    _handle(payload);\n}' }],
  'xchain',
  [{ q: 'What does a light-client bridge verify?', options: ['A remote chain consensus proof according to its client rules', 'That every application payload is safe', 'That no reorg can happen', 'Only a multisig signature'], answer: 0, why: 'The light client verifies consensus evidence. The application still needs sender, replay and payload authorization checks.' }, { q: 'Why include source chain and nonce in a message ID?', options: ['To lower gas fees', 'To stop the same payload being accepted again or in the wrong domain', 'To make messages private', 'To speed finality'], answer: 1, why: 'Payloads can be identical. Domain-separated identifiers make the intended origin and one-time consumption explicit.' }, { q: 'What does a fast bridge usually add?', options: ['Instant protocol finality', 'A liquidity provider that fronts funds and assumes the settlement wait', 'A lighter validator set automatically', 'No bridge trust assumptions'], answer: 1, why: 'The user receives funds early because someone else supplies inventory, then settles later. That is a liquidity and counterparty trade-off.' }],
  ['Use the lab to replay a message and verify that the receiver rejects it.', 'Write a remote-message schema with source domain, nonce, expiry and payload version.', 'Choose a bridge model for a treasury transfer and state its trust assumption.'],
  [{ type: 'docs', title: 'Ethereum.org — bridges', url: 'https://ethereum.org/en/bridges/' }, { type: 'read', title: 'L2BEAT bridge risk framework', url: 'https://l2beat.com/bridges/' }]);

lesson('advanced-aa', 50, 'Account Abstraction and Smart Wallets', 75,
  'Use programmable accounts to batch actions, sponsor gas and constrain delegated authority without turning a wallet into an unrestricted hot key.',
  ['Explain the ERC-4337 user-operation lifecycle', 'Separate account validation, paymaster policy and execution', 'Design session keys with target, value and expiry limits', 'Model recovery and nonce handling for smart wallets'],
  `<h3>A user operation is not an EOA transaction</h3><p>In ERC-4337, a wallet submits a <code>UserOperation</code> to a bundler. The EntryPoint validates the account signature and, when used, a paymaster policy, then executes the account call. This makes batching, gas sponsorship, social recovery and alternative signing rules possible without changing Ethereum consensus.</p>
<h3>Delegation needs narrow authority</h3><p>A session key should state which contracts it may call, which selectors, maximum value or token spend, expiry and nonce space. “A key that can call anything for a week” is a hot wallet with a nicer name. Validate policy on chain and make revocation immediate. Paymasters need their own controls: allowlisted targets, bounded gas, rate limits and anti-sybil economics.</p>
<h3>Recovery changes the trust model</h3><p>Guardians, passkeys and recovery modules improve usability but add paths to account control. Define quorum, delays, cancellation and notifications. Smart accounts are programmable security boundaries; audit their modules and upgrade authority as carefully as a DeFi protocol.</p>`,
  [{ lang: 'solidity', file: 'SessionPolicy.sol', caption: 'A session key policy constrains the capability rather than merely checking that a key exists.', src: 'struct Session { uint48 expiresAt; uint96 maxValue; bool revoked; }\nmapping(address => Session) sessions;\n\nfunction validateSession(address key, address target, bytes4 selector, uint256 value) internal view {\n    Session memory s = sessions[key];\n    require(!s.revoked && block.timestamp < s.expiresAt, "session invalid");\n    require(target == game && selector == game.play.selector, "target denied");\n    require(value <= s.maxValue, "value cap");\n}' }],
  'smartwallet',
  [{ q: 'What validates a UserOperation in ERC-4337?', options: ['Only a wallet UI', 'The account and optional paymaster through the EntryPoint flow', 'A block explorer', 'The token contract'], answer: 1, why: 'The account defines signature or policy validation; the paymaster separately decides whether to sponsor gas. Bundlers package the operation for on-chain processing.' }, { q: 'Which session-key design has the smallest blast radius?', options: ['Unlimited calls until tomorrow', 'A key limited to one target, selector, spend cap and expiry', 'The owner key copied to a game server', 'A key with no revocation'], answer: 1, why: 'Capabilities should be specific and short-lived. Every omitted limit expands what a compromise can do.' }, { q: 'What is a paymaster policy for?', options: ['Changing block finality', 'Deciding which user operations the sponsor will pay for', 'Replacing account validation', 'Hiding all transactions'], answer: 1, why: 'The paymaster pays gas under rules it defines. It must not be an unbounded public gas faucet.' }],
  ['Use the lab to issue a session key and attempt calls outside its policy.', 'Design a recovery quorum, delay and cancellation path for a family wallet.', 'Write a paymaster policy for a free mint that cannot be farmed indefinitely.'],
  [{ type: 'eip', title: 'ERC-4337', url: 'https://eips.ethereum.org/EIPS/eip-4337' }, { type: 'docs', title: 'Ethereum.org — account abstraction', url: 'https://ethereum.org/en/roadmap/account-abstraction/' }]);

lesson('advanced-crypto', 51, 'Cryptography in Production: KZG, BLS and Threshold Keys', 80,
  'Choose modern cryptographic building blocks by their trust and operational properties, not by their acronyms.',
  ['Explain what KZG commitments prove in Ethereum data availability', 'Compare BLS aggregation with ordinary signature collection', 'Identify threshold-signing custody trade-offs', 'Plan key rotation, ceremony and cryptographic-agility controls'],
  `<h3>Commitments make large data cheaply checkable</h3><p>KZG commitments let a prover commit to a polynomial and later prove an evaluation at one point without revealing all coefficients. Ethereum uses them for blob commitments: consensus can verify that blob data matches a small commitment. The trusted setup is a real assumption; a multi-party ceremony makes it safe if at least one contributor destroyed their secret contribution.</p>
<h3>Aggregation changes the bandwidth equation</h3><p>BLS signatures can aggregate many validator signatures into one compact signature, which is why they are useful for consensus attestations. Aggregation does not remove the need to validate the signer set, message domain and participation bitmap. A valid aggregate over the wrong message is still the wrong authorization.</p>
<h3>Threshold custody is an operations system</h3><p>MPC and threshold schemes can require several devices or operators to cooperate without reconstructing a private key in one place. They introduce coordinator availability, participant replacement, backup and recovery complexity. Define key generation, resharing, rotation and emergency policy before funds depend on the scheme. Cryptographic agility means recording algorithms and domains so future migration is possible rather than assuming today’s curve lasts forever.</p>`,
  [{ lang: 'javascript', file: 'domain-separated-digest.js', caption: 'Signatures should bind an explicit protocol domain and chain context, not only an ambiguous payload.', src: 'function digest(domain, chainId, payloadHash) {\n  return keccak256(abi.encode(domain, chainId, payloadHash));\n}\n\n// Different domains prevent an authorization for one protocol or chain\n// from becoming a valid signature in another.\nconst withdrawal = digest("TREASURY_WITHDRAWAL_V1", 1, requestHash);\nconst governance = digest("GOVERNANCE_VOTE_V1", 1, requestHash);' }],
  'cryptops',
  [{ q: 'What does a KZG commitment allow?', options: ['A short proof that a claimed value belongs to committed polynomial data', 'Encryption of every blob forever', 'A replacement for data availability', 'A private validator key'], answer: 0, why: 'Commitments make consistency proofs succinct. Availability still requires the data to be published where participants can obtain it.' }, { q: 'What must a verifier check for a BLS aggregate?', options: ['Only signature length', 'Signer set, message domain and aggregate signature', 'The signer IP addresses', 'A token balance'], answer: 1, why: 'Aggregation compresses signatures, not authorization semantics. The verifier must know exactly who signed exactly what.' }, { q: 'What is a threshold-signing trade-off?', options: ['It removes all operational risk', 'It distributes custody but adds participant, recovery and coordinator complexity', 'It makes key rotation impossible', 'It needs no policy'], answer: 1, why: 'No single key holder may be able to sign, which reduces one risk while creating availability and lifecycle work that must be designed.' }],
  ['Use the lab to compare a single key, multisig and threshold workflow during one participant outage.', 'Write domain strings and versioning rules for every signature in a protocol.', 'Create a key-ceremony runbook with rotation, resharing and emergency recovery.'],
  [{ type: 'eip', title: 'EIP-4844 blobs and KZG commitments', url: 'https://eips.ethereum.org/EIPS/eip-4844' }, { type: 'docs', title: 'Ethereum.org — BLS signatures', url: 'https://ethereum.org/en/developers/docs/consensus-mechanisms/pos/attestations/' }]);

lesson('advanced-indexer', 52, 'Blockchain Data Engineering and Reorg-Safe Indexing', 80,
  'Build an indexer that treats blocks as a reversible stream, preserves provenance, and gives applications fast answers without lying during reorgs.',
  ['Design an ingestion pipeline with raw, decoded and derived layers', 'Handle confirmations, reorg rollback and idempotent writes', 'Choose event schemas and query indexes for product use cases', 'Monitor freshness, gaps and decode failures'],
  `<h3>Indexing is a derived view of consensus</h3><p>Applications need queries that nodes do not optimize for: all swaps by user, portfolio history, NFT activity or protocol TVL over time. An indexer consumes blocks and events, stores raw provenance, decodes them into tables, then builds product views. It must never pretend its database is more final than the source chain.</p>
<h3>Reorgs make the stream reversible</h3><p>Store block hash, parent hash, height, transaction hash and log index with every record. Mark data provisional until the chain-specific confirmation threshold, or maintain a canonical flag that can be rolled back to the common ancestor. Writes must be idempotent on a stable event key; retries are normal. A pipeline that only appends will eventually show users events from a chain branch that no longer exists.</p>
<h3>Operate the data product</h3><p>Track head lag, missing block ranges, RPC error rate, decode failures by contract version and reconciliation differences against authoritative state. Version ABI decoders and event schemas. Use a dead-letter queue for unknown data instead of silently dropping it. For money-moving decisions, query the chain directly or require sufficient finality; an indexer is a performance layer, not an authorization oracle.</p>`,
  [{ lang: 'sql', file: 'events.sql', caption: 'A canonical flag and a unique event identity make rollback and retry explicit.', src: 'create table decoded_events (\n  chain_id bigint not null,\n  block_number bigint not null,\n  block_hash bytea not null,\n  tx_hash bytea not null,\n  log_index integer not null,\n  canonical boolean not null default true,\n  event_name text not null,\n  payload jsonb not null,\n  primary key (chain_id, tx_hash, log_index)\n);\n\n-- On reorg: mark events after the common ancestor non-canonical, then ingest\n-- the replacement branch. Do not delete raw provenance you need to explain it.' }],
  'indexer',
  [{ q: 'Why store block hash as well as block number?', options: ['To make SQL longer', 'A reorg can replace a block at the same height with a different hash', 'Block numbers are secret', 'It removes the need for confirmations'], answer: 1, why: 'Height alone does not identify a unique chain history. Hash and parent linkage let the indexer detect and roll back a replaced branch.' }, { q: 'What makes an event write idempotent?', options: ['A random retry ID', 'A stable unique key such as chain ID, transaction hash and log index', 'Deleting all tables first', 'Ignoring duplicates'], answer: 1, why: 'The same block may be fetched again. Stable identity lets retries produce the same result without duplicate rows.' }, { q: 'When should an application query chain state directly?', options: ['For a money-moving authorization decision', 'Only for CSS styling', 'Never, indexers are consensus', 'Only after a reorg'], answer: 0, why: 'An indexer can lag, fail or reflect provisional data. Authoritative contract state and finality rules belong on the execution path.' }],
  ['Use the lab to ingest blocks, cause a reorg and verify that the old branch becomes non-canonical.', 'Design raw, decoded and product-view tables for an AMM analytics page.', 'Add alerts for head lag, missing ranges and ABI decode failures.'],
  [{ type: 'docs', title: 'The Graph indexing architecture', url: 'https://thegraph.com/docs/en/indexing/overview/' }, { type: 'docs', title: 'Ethereum JSON-RPC', url: 'https://ethereum.org/en/developers/docs/apis/json-rpc/' }]);

})(window.ROADMAP.lessons);
